{
  "name": "platform",
  "projectType": "library",
  "sourceRoot": "packages/platform",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config packages/platform/jest.config.cjs --runInBand"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/platform --ext .ts"
      }
    }
  }
}
