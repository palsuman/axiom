{
  "name": "icons",
  "projectType": "library",
  "sourceRoot": "packages/icons",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config packages/icons/jest.config.cjs --runInBand"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/icons --ext .ts"
      }
    }
  }
}
